# WB-STREAM-001 CP2 — contract mock backend dependencies.
# Locked versions (Python 3.13, Windows). Install into the project-local venv:
#   python -m venv backend/.venv
#   backend/.venv/Scripts/pip install -r backend/requirements.txt
# Do NOT install into the ESP-IDF Python or system Python. The venv itself is
# git-ignored (**.venv/).

# --- runtime ---
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- test ---
pytest==9.1.1
httpx==0.28.1

# --- transitive (locked for reproducibility) ---
anyio==4.14.2
pydantic==2.13.5
starlette==1.6.0
h11==0.16.0
httpcore==1.0.9
idna==3.19
click==8.5.0
colorama==0.4.6
httptools==0.8.0
python-dotenv==1.2.3
watchfiles==1.2.0
websockets==17.1
PyYAML==6.0.3
Pygments==2.21.0
annotated-doc==0.0.5
annotated-types==0.8.0
certifi==2026.7.22
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pydantic_core==2.46.5
typing-inspection==0.4.4
typing_extensions==4.16.0
