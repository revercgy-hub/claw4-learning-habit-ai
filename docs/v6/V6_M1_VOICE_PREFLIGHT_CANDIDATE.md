# S-04 M1 Voice Preflight preparation

Status (2026-09-25 continuation): **BUILD_COMPLETE / R-02_REVIEW_REQUIRED / DEVICE_NOT_TESTED**. The prior `BLOCKED_DATA_LOSS_RISK` finding below is **SUPERSEDED for the new M1 staged source only** by the fail-closed NVS initialization patch at source commit `6e3ed87d987a2646b0fbefe382d92e4a06ef7eee`. Candidate identity and exact hashes are in [`integration/v6/m1-candidate-preflight.json`](../../integration/v6/m1-candidate-preflight.json), candidate `claw4-learning-v6-m1-preflight-s04-20260925-01`. No flashing or device use occurred. R-02 must review before S-05 receives hardware ownership. M0 remains `CHANGES_REQUIRED`; Candidate20 AP outage/recovery remains `SKIPPED_BY_USER / NOT_VERIFIED`.

## 2026-09-25 continuation from the stopped checkpoint

- Worktree `E:/workbuddy/claw4-m1-s04`, branch `codex/v6-m1-s04`, continued from clean HEAD `4fe11ec` (base `5849e6e`), not from main or Candidate08. The old `E:/v6/m1-s04` stage was not overwritten. A fresh `E:/v6/m1-s04-safe-pinned` stage contains the exact pinned XiaoZhi upstream `ac6deed3d8e75348475364bf40ad953c6cd48054` and ESP-IDF `fff9895c82d744c7237be8847347bdd1b07c6643`.
- M1 staging now requires the exact upstream `main/main.cc` NVS-erase block once and replaces it with `nvs_flash_init(); ESP_ERROR_CHECK(ret);`. Any missing or duplicate anchor aborts. On `ESP_ERR_NVS_NO_FREE_PAGES` or `ESP_ERR_NVS_NEW_VERSION_FOUND`, boot fails closed rather than erasing live NVS. M0 staging remains on its previous path. This is source/build protection, not an NVS recovery procedure.
- Upstream `AudioService::CheckAndUpdateAudioPowerState()` can retain TX while duplex RX is enabled. Consequently output may remain enabled across voice utterances and the reference queue may retain prior samples. The Claw4 adapter now ends its reference session after 250 ms without an accepted TX write, both when RX resumes and before a new TX burst; in-flight writes are exempt. The host burst test checks a 100 ms inter-frame gap retains the session and a 300 ms gap discards the old sample. This bounds stale reference lifetime; no playback/Wake ownership moved from XiaoZhi, and no AEC acoustic effectiveness is claimed. A next utterance within 250 ms still requires device observation for reference alignment and self-dialogue.
- Fresh source initially resolved 81 dependencies without a seed lock, changing `esp_wifi_remote` 1.6.4→1.6.5, `sh1106-esp-idf` 1.0.1→1.0.2 and omitting `esp_h264`; that non-pinned attempt was stopped before build completion and is **not** the Candidate. The new M1 stage seeds `integration/v6/idf61-components.lock` SHA-256 `c811bfe049e3c99fa129fa3050d4807955db7d0aa90e6e97a42e84034f158a39`, then resolves all 82 pinned entries. The 81 managed component directories plus IDF 6.1.0 preserve every pinned version. All nonlocal component hashes remain exact. The local Wi-Fi override necessarily replaces the registry hash/source metadata for `78/esp-wifi-connect`; its 23 generated files are individually hashed in the Candidate manifest.
- The final `dependencies.lock` SHA-256 is `18efb5df229b4e429b9a2119a4b069346ef4a7d5ea180fdfcee97ac1c87661a2`. The exact allowlisted diff is: `78/esp-wifi-connect` source `service`→`components/78__esp-wifi-connect` local, with registry component hash and transitive dependency metadata removed; `espressif/cjson` registry URL gains a trailing slash and becomes a direct dependency; `manifest_hash` changes for that local manifest. Any other byte or pin drift fails the M1 build guard. The initial wrapper completed the second build but exited 1 at its earlier byte-equality guard; a same-tree incremental `reconfigure build` using the exact allowlist exited 0. Logs and both hashes are bound in the Candidate manifest.
- Final `sdkconfig` selects the live `claw4-live-m1.csv`, `CONFIG_CLAW4_M0_DIAGNOSTICS` off, device AEC on, 32 MB DIO/40 MHz, and the private LAN OTA endpoint. `build/project_description.json` selects `E:/v6/m1-s04-safe-pinned/components/78__esp-wifi-connect`. Final `compile_commands.json` includes patched `main/main.cc` and `claw4_audio.cc`. The generated 3,072-byte partition-table binary SHA-256 `ef0039b6366c57de098972c0f6e9fd991013b41968da9b866c4704cb68ef7e5f` is byte-equal to the earlier generated table that was proven to match the first 3,072 bytes of S-03's live readout; nothing was written to the device.
- App `build/xiaozhi.bin`: 3,207,616 bytes, SHA-256 `da3495314b6c01b138b2433bbc94489a2212bee70afa02d4244933fa8f7a3509`. ELF `build/xiaozhi.elf`: 48,409,240 bytes, SHA-256 `a5c773bc9204aad2f3e03200d197d78a44bfc6460de189484b6c9ba516a1c6a8`. IDF reports the app fits the smallest 4 MiB app partition with 24% spare. The full Preflight manifest records source/tool/config/artifact hashes and remains `BUILD_COMPLETE_REVIEW_REQUIRED`, not DEVICE PASS or M1 PASS.
- Candidate manifest SHA-256 (committed LF bytes): `36fbb1ad3d9fe73128c5d1c2586001197b8d2eab0b4376ee8c5f2412dd65b050`. Its `status` is `BUILD_COMPLETE_REVIEW_REQUIRED`, and `device_validation` is `NOT_RUN`.
- Host checks: `g++.exe -std=c++17 -Wall -Wextra -Werror tools/v6/test_board_algorithms.cc` plus executable: PASS; `py -3.14 -B -m unittest discover -s tools/v6 -p 'test_*.py'`: **114/114 PASS**; pinned IDF 6.1 Python 3.12 build plus final incremental `reconfigure build`: PASS; `git diff --check`: PASS. No COM7 or NAS access, no Flash/NVS recovery/bootloader/`ota_1`/partition-table write, and no `E:/v6/s1` operation occurred.

Review focus for R-02: inspect the exact NVS anchor/patched main source, the byte-constrained local-lock rewrite, and TX idle boundary against real upstream playback timing. Device-only unknowns remain Voice startup, Wake, AEC/reference timing, playback-period near-end behavior, TTS self-recapture, and resource trend. S-05 stays closed until an independent R-02 PASS.

## Historical stop evidence (SUPERSEDED for this new stage)

## Blocking source finding

The pinned XiaoZhi v2.5.0 upstream `main/main.cc:17-21` initializes NVS and, when `nvs_flash_init()` returns `ESP_ERR_NVS_NO_FREE_PAGES` or `ESP_ERR_NVS_NEW_VERSION_FOUND`, calls `nvs_flash_erase()` before trying again. The S-04 stage contains those exact lines unchanged. A Voice Preflight boot could therefore erase live NVS data upon either error. Editing that upstream entry point or changing NVS policy is outside this task's file whitelist. Astra stopped candidate progression on this data-loss risk before any device operation. A separately reviewed, bounded decision and implementation are required before building a flashable Candidate.

## Live layout and proposed build-only inputs

S-03's private read-only `device-partition-before.bin` is 4,096 bytes, SHA-256 `8e5a526458f90a2b32a28ca0162611ca680e7ac8ca49f8336999ad85b529521a`. ESP-IDF `gen_esp32part.py` decoded `otadata` at `0x10e000/8K`, `phy_init` at `0x110000/4K`, `model` at `0x111000/956K`, `ota_0` at `0x200000/9M`, `ota_1` at `0xb00000/4M`, and `resources` at `0xf00000/4M`, plus the remaining partitions in `integration/v6/board/claw4-live-m1.csv`. Generating the binary from that CSV gave 3,072 bytes exactly equal to the first 3,072 bytes of the live table; the remaining live 1,024 bytes were not written. The existing `claw4-v6.csv` was not changed. This is build configuration, **not** authorization or a plan to write a partition table.

An additive M1 board build entry selects the live CSV, disables the M0 diagnostic entry point, retains device AEC, and points `CONFIG_OTA_URL` at plain HTTP `http://192.168.3.100:7443/xiaozhi/ota/`. The deployed OTA response supplies `ws://192.168.3.100:7444/xiaozhi/v1/`; WebSocket URL is an upstream persisted setting loaded from that response, not a separate build flag. Upstream `Application`, `AudioService`, AFE and protocol remain the single Voice owner. No Learning UI or M1.5 router was added.

The proposed board adapter feeds accepted post-volume TX samples into the AFE reference slot even with M0 diagnostics disabled, with bounded queue/session reset and periodic queue telemetry. Candidate20's physical RX reference slot was silent. The zero-frame software offset remains uncalibrated; this change does **not** prove effective acoustic cancellation or safe near-end speech. Upstream playback and Wake lifecycle remain owned by XiaoZhi. For a later hardware Preflight, record whether TTS is recaptured, whether self-dialogue starts, near-end speech during playback, Wake rearm, crash/WDT/reboot, and heap/PSRAM trend.

Upstream `main/assets.cc:20,43-47` looks specifically for an `assets` label while the live table contains `resources`. `Application::CheckAssetsVersion()` returns when the assets partition is invalid (`main/application.cc:390-394`). The AFE fallback independently initializes models by `esp_srmodel_init("model")` (`main/audio/engines/afe_audio_engine.cc:65-68`), matching the live label. Thus the label mismatch is an assets/theme gap and is not by itself evidence that Voice startup must fail; only a device run can confirm startup and Wake.

## Offline evidence and limits

- Source base: `5849e6e6ac3c3bffb3a7a1ad6bfcf5921fa08a94`. Pinned upstream XiaoZhi SHA `ac6deed3d8e75348475364bf40ad953c6cd48054`; ESP-IDF SHA `fff9895c82d744c7237be8847347bdd1b07c6643`.
- `py -3.14 -B tools/v6/stage_device.py --upstream E:/workbuddy/claw4-v6/vendor/xiaozhi-esp32 --destination E:/v6/m1-s04 --variant m1`: PASS. Private stage manifest SHA-256 `c5822f76bd49614dbdbe50f1802c0f0c8a9dc870a74839779b297e9acdcd109b` is a staging trace, not a frozen Candidate manifest.
- `E:/workbuddy/toolchains/w64devkit-2.9.1/bin/g++.exe -std=c++17 -Wall -Wextra -Werror tools/v6/test_board_algorithms.cc` with that compiler's `bin` in PATH, followed by execution: PASS.
- `py -3.14 -B -m unittest discover -s tools/v6 -p 'test_*.py'`: 109 tests, 0 failures/errors. Private log `E:/v6/m1-s04-host-tests.log` SHA-256 `16c538142a9481ffa23b716490ac7949051d23d026349afcbc2fcb0a1ece9280`.
- Pinned IDF 6.1 Python 3.12 build was started in private `E:/v6/m1-s04` and interrupted by Astra's data-loss stop while managed components were resolving (last log entry `lvgl/lvgl (9.5.0)`). Private `E:/v6/m1-s04-build.log` SHA-256 `778262f575f2460c6988265ac9116ccd844f151dee50ad86445f6499f10839af`. No `sdkconfig`, app binary or ELF was produced. The earlier Python 3.14 attempt failed before configuration because the pinned IDF environment is Python 3.12; its log was overwritten by the pinned attempt.
- No source/image/ELF identity or build output is frozen, no preflight candidate JSON was created, and no DEVICE PASS or M1 PASS is claimed. No COM7, flash, NVS, C5, bootloader, `ota_1`, partition table, or shared `E:/v6/s1` operation occurred.

Next decision: address the upstream automatic NVS erase path under a separately reviewed scope, then resume a clean pinned build, bind exact input/app/ELF hashes and a unique Candidate manifest, seek independent R-02, and only after PASS hand the device to the sole S-05 owner for 2-3 Preflight rounds. A stable Preflight would still not be M1 PASS; formal 20 rounds would start again at round 1 after any firmware change.
