# CLAW4 主机准备报告 — WorkBuddy 复核结论

- 复核时间: 2026-09-01 14:55
- 复核对象: `docs/CLAW4_主机准备情况报告_2026-09-01.md` (codex 产出)
- 复核方式: 逐条实际执行报告第 6 节复核命令 + 额外交叉验证
- **总体结论: 报告真实可信, 主机软件层 READY 判定成立**

---

## 一、逐项复核结果

### 6.1 路径存在性 — 6/6 全部通过 ✅

| 路径 | 结果 |
| --- | --- |
| `toolchains\esp-idf-v5.5.4` | ✅ 存在 |
| `toolchains\.espressif` | ✅ 存在 (报告未提及, 见第三节发现) |
| `E:\workbuddy\claw4-idf-tools` | ✅ 存在 (含 tools/ python_env/ idf-env.json) |
| `E:\c` | ✅ 存在 (Claw4 构建镜像, 无 .git 符合说明) |
| `E:\i` | ✅ 存在 (IDF 构建镜像) |
| `E:\b\xiaozhi.bin` | ✅ 存在 |

### 6.2 工具版本 — 4/4 全部匹配 ✅

| 工具 | 报告声称 | 实测 | 结果 |
| --- | --- | --- | --- |
| riscv32-esp-elf-gcc | 14.2.0 (esp-14.2.0_20260121) | 14.2.0 crosstool-NG esp-14.2.0_20260121 | ✅ |
| cmake | 3.30.2 | 3.30.2 | ✅ |
| ninja | 1.12.1 | 1.12.1 | ✅ |
| IDF Python | 3.12.13 | Python 3.12.13 | ✅ |

另确认 tools 目录含 ccache / esp-rom-elfs / idf-exe / openocd-esp32 / riscv32-esp-elf-gdb。

### 6.3 ESP-IDF 与 Python 依赖 ✅

- `idf.py --version` → **ESP-IDF v5.5.4** (E:\i 无 .git 产生的 Git 警告符合报告说明, 可忽略)
- `check_python_dependencies.py` → **Python requirements are satisfied**

### 6.4 固件产物 — 5/5 全部匹配 ✅

| 产物 | 报告大小 | 实测大小 | 结果 |
| --- | ---: | ---: | --- |
| xiaozhi.bin | 9,036,192 | 9,036,192 | ✅ |
| bootloader.bin | 20,416 | 20,416 | ✅ |
| partition-table.bin | 3,072 | 3,072 | ✅ |
| factory_test.bin | 614,400 | 614,400 | ✅ |
| Metalio_Claw4_Latest.bin | 33,120,256 | 33,120,256 | ✅ |

### 额外交叉验证 ✅

- 官方 commit: `ca3aa3fa027ff7dad2adf0c2d03c4f24aa838950` 实测一致; 工作树干净 (无未跟踪修改)
- `E:\c` 与 vendor 副本关键文件 (CMakeLists/README/dependencies.lock) 一致; esp_claw_bin 分区固件齐全
- 出厂固件两份副本 SHA256 前缀一致 → 同一文件
- CMakeCache.txt 实锤构建环境: `CMAKE_MAKE_PROGRAM=E:/workbuddy/claw4-idf-tools/.../ninja.exe`, `PYTHON=...claw4-idf-tools/python_env/...`, `IDF_TARGET=esp32p4`
- OTA 警告属实: 分区表 ota_0=9M / ota_1=4M, xiaozhi.bin ≈ 8.62 MiB → 放不进 ota_1 (溢出 0x49e1a0)

---

## 二、对上一轮 WorkBuddy 检查的修正

上一轮 (docs/系统检查报告_2026-09-01.md) 曾判定 **"IDF 工具链未安装 (P0)"**, 依据是默认路径 `C:\Users\rever\.espressif` 不存在。

**该判定错误, 特此修正:**

- codex 采用**自定义 `IDF_TOOLS_PATH = E:\workbuddy\claw4-idf-tools`**, 工具链/Python 环境完整安装于此;
- `~/.espressif` 不存在是**预期行为**, 不是缺漏;
- 基线编译已实际完成 (E:\b 产物齐全, CMakeCache 可查);
- 短路径 `E:\c / E:\i / E:\b` 方案合理, 规避了中文路径与命令长度问题。

---

## 三、复核中发现的新情况 (报告未提及)

### 1. 存在两套 IDF 工具安装 (冗余, 建议清理)

| 位置 | 大小 | 说明 |
| --- | --- | --- |
| `E:\workbuddy\claw4-idf-tools` | 2.8 GB | **实际生效** (CMakeCache 指向此) |
| `toolchains\.espressif` | 3.5 GB | 冗余副本, 含 dist 下载缓存 |

两套 `idf-env.json` 内容与时间戳完全一致 → 疑似安装时先写入 `.espressif` 后复制/重建于 `claw4-idf-tools`, 或为规避长路径重建所致。

**建议:** 确认后续无脚本依赖 `.espressif` 后, 可删除 `toolchains\.espressif` 释放 ~3.5 GB。删除前需用户确认。

### 2. idf-env.json 注册路径与构建路径不一致 (低风险)

- idf-env.json 注册: `toolchains\esp-idf-v5.5.4` (原始源码)
- 实际构建 IDF_PATH: `E:\i` (短路径镜像, 无 .git)

两者同为 v5.5.4, 版本一致, 不影响构建; 仅提示: 若用 `export.ps1` 自动激活, 拿到的是原始路径而非短路径, 命令长度风险仍存在。

---

## 四、复核判定

- ✅ 报告全部可验证项**属实**, 无虚构、无夸大
- ✅ 主机软件层 READY: 源码固定、工具链齐全、基线编译通过、出厂固件在位
- ✅ PENDING / DEVICE_VERIFY_REQUIRED 项 (串口/驱动/屏幕/触摸/C5) 分类**合理**, 均属真机到货后才能确认
- ⚠️ 唯一新增建议: 清理冗余工具副本 `toolchains\.espressif` (3.5 GB)
- ⚠️ 真机到货前无需再装任何软件; 到货后按报告第 8 节流程执行

**最终判定: 报告通过复核, 可进入真机阶段等待。**
